---
export const prerender = false;

const url = new URL(Astro.request.url);
const query = url.searchParams.get("q");

return Astro.redirect(query ? `/learn/search/1?q=${encodeURIComponent(query)}` : "/learn/search/1");
---