---
export const prerender = false;

const url = new URL(Astro.request.url);
const query = url.searchParams.get("q");

if (!query) {
	return Astro.redirect("/resources");
}

return Astro.redirect(`/resources/search/1?q=${encodeURIComponent(query)}`);
---

